# examples

Concrete end-to-end example applications.

These packages are where the shared `gs_utils` pieces are composed into real
trainers, viewers, and export flows. They are also the place to prototype
concrete behavior before generalizing it upward.

In practice this is the layer that pressure-tests the architecture. Once a
pattern works cleanly here, it is a candidate to move into the shared packages.
