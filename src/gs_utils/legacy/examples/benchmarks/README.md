# legacy/examples/benchmarks

Legacy benchmark launch scripts and result helpers.

This directory contains older experiment scripts and summary utilities. It is
useful as historical reference, not as the long-term interface for running
evaluations.
