# legacy/examples

Legacy end-to-end reference applications.

These scripts are kept as a source of truth for behavior and feature parity
while the shared `gs_utils` implementation is being rebuilt in a cleaner form.
