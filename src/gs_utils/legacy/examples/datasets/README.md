# legacy/examples/datasets

Legacy dataset helpers used by the old example scripts.

These files predate the current shared data layer and mainly serve as reference
material while functionality is ported into `gs_utils.data`.
