# export

Geometry export helpers.

This package contains one-way export utilities for shared geometry contracts.
Right now the focus is gsplat-compatible PLY export.

The intent is to keep file-format boundaries here, rather than letting external
format naming or storage conventions leak back into the internal scene
contracts.
