# data/colmap

COLMAP-backed parsing and normalization helpers.

This package contains the concrete COLMAP reader and related utilities used to
turn reconstructed scenes into the shared parsed-scene format used elsewhere in
`gs_utils`.
